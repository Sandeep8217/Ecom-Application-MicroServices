spring:
  application:
    name: product-service

  datasource:
    url: jdbc:postgresql://localhost:5432/productdb?options=-c%20TimeZone=Asia/Kolkata
    username: sandeep
    password: sandeep
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#  h2:
#    console:
#      enabled: true
#
#  datasource:
#    url: jdbc:h2:mem:test
#
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: create

server:
  port: 8081
